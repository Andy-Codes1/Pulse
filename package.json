{
  "name": "pulse-mvp",
  "private": true,
  "version": "1.0.0",
  "description": "Pulse P2P Remittance MVP — AI-powered cross-border payments",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ios:init": "npx cap add ios",
    "ios:sync": "vite build && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "vite build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
